name: Deploy

on:
  push:
    branches: [main]
    paths-ignore:
      - 'README.md'
  workflow_dispatch: {}

jobs:
  build:
    uses: jackowfish/server-utilities/.github/workflows/build-image.yml@master
    with:
      image: ghcr.io/jackowfish/guess-room
